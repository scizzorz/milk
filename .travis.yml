language: rust

rust:
  - stable
  - beta
  - nightly

matrix:
  allow_failures:
    - rust: nightly
    - rust: beta
  fast_finish: true


before_script:
  - rustup component add clippy

script:
  - cargo build --verbose
  - cargo clippy -- -D warnings
